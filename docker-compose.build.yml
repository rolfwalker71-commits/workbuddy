# Local source build only. Do not use on the Paperless host.
# docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  workbuddy:
    build:
      context: .
      dockerfile: Dockerfile
